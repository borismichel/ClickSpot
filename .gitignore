.env
.venv/
__pycache__/
*.pyc
.dagster/
.dagster_home/
.clickhouse/
*.egg-info/
.DS_Store
.tmp_dagster_home_*/
node_modules/
frontend/dist/

# Per-portal silver extension (local to each install)
silver_config_custom.py

# Local README render scratch (visual-truth gate; not for the public repo)
.readme-render/
.render-*.cjs

# MkDocs build output (published to GitHub Pages by CI, not committed)
site/
docker-compose.override.yml

# Agent-skill plumbing — per-developer config for the engineering skills
# (issue tracker, triage vocabulary, domain-doc layout). Local to each clone,
# regenerated by /setup-matt-pocock-skills; not for the public repo.
.scratch/
docs/agents/
